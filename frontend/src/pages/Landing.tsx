/**
 * Página Landing — apresentação pública do grain.
 * Implementada em detalhe no Passo 4.1.
 * Por agora: placeholder funcional para teste de rotas.
 */

export default function Landing() {
  return (
    <div style={{ padding: '40px', fontFamily: 'DM Sans, sans-serif', color: '#f0ece4' }}>
      <h1 style={{ fontFamily: 'Syne, sans-serif', color: '#c8a96e' }}>grain</h1>
      <p style={{ color: '#555' }}>no noise, only grain — landing page (Passo 4.1)</p>
    </div>
  );
}
