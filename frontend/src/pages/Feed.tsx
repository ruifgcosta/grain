/**
 * Página Feed — lista cronológica de artigos.
 * Implementada em detalhe no Passo 3.5.
 */

export default function Feed() {
  return (
    <div style={{ padding: '20px', color: '#f0ece4' }}>
      <p style={{ color: '#555', fontSize: '12px' }}>Feed (Passo 3.5)</p>
    </div>
  );
}
